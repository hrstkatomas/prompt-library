# Worktree script
Will create new worktree for existing branch, will link node_modules, vendors and ide index data from original repository

## Add alias into your .bashrc
`alias wt='bash <prompt-library-path>/engineering/worktrees/new-worktree.sh'`
